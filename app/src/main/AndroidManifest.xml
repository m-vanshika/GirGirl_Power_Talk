<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.girlpowertalk">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.GirlPowerTalk">
        <activity android:name=".JudgeQues"></activity>
        <activity android:name=".SubQues" />
        <activity android:name=".ChangeQuestions" />
        <activity android:name=".Progress" />
        <activity android:name=".userpage" />
        <activity android:name=".adminpage" />
        <activity android:name=".Signin" />
        <activity android:name=".Hompage" />
        <activity
            android:name=".Login"
            android:label="@string/title_activity_login"
            android:theme="@style/Theme.GirlPowerTalk.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>